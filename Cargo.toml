[workspace]
members = [
    "gcp",
    "mpc-recovery",
    "contract",
    "node",
    "integration-tests",
    "load-tests",
    "keys",
    "test-oidc-provider",
]

[profile.flamegraph]
inherits = "release"
debug = true
